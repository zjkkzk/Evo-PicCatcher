<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Shizuku/Sui 权限 -->
    <uses-permission android:name="moe.shizuku.privileged.api.permission.RECEIVE_SUI_BINDER" />
    <!-- 遍历 Android/data 必需 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- 前台服务必备 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PicCatcher"
        android:usesCleartextTraffic="true">

        <provider
            android:name="com.pic.catcher.provider.LogProvider"
            android:authorities="com.pic.catcher.logprovider"
            android:exported="true"
            android:enabled="true"
            tools:ignore="ExportedContentProvider">
            <intent-filter>
                <action android:name="android.content.action.LOG_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true" />

        <meta-data
            android:name="rikka.shizuku.api.VERSION"
            android:value="13" />

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="@string/app_xposed_description" />
        <meta-data android:name="xposedminversion" android:value="93" />
        <meta-data android:name="xposedscope" android:resource="@array/xposed_scope" />

        <service
            android:name=".service.AppTileService"
            android:label="@string/app_name"
            android:icon="@drawable/ic_icon_des"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".service.PicWatcherService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitoring and harvesting images from host app cache directories to centralized storage via Shell/Shizuku." />
        </service>

        <activity
            android:name="com.pic.catcher.ui.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.pic.catcher.ui.DeepLinkActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.PicCatcher.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <data android:host="com.pic.catcher" android:scheme="piccatcher" />
            </intent-filter>
        </activity>

        <activity android:name="com.pic.catcher.ui.WebViewActivity" android:exported="false" />
        <activity
            android:name="com.pic.catcher.ui.ConfigActivity"
            android:theme="@style/Theme.PicCatcher.NoActionBar"
            android:exported="false" />
    </application>
</manifest>
