<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PicCatcher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Material You will override these if enabled in code -->
        <item name="colorPrimary">@color/app_primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- 移除硬编码，让 DayNight 主题自动控制状态栏图标颜色 -->
    </style>

    <style name="Theme.PicCatcher.NoActionBar" parent="Theme.PicCatcher" />

    <style name="Theme.PicCatcher.Transparent" parent="Theme.PicCatcher">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <!-- Custom Chip Style -->
    <style name="Widget.Material3.Chip.Suggestion.Thin" parent="Widget.Material3.Chip.Suggestion">
        <item name="chipMinTouchTargetSize">0dp</item>
        <item name="android:layout_height">32dp</item>
    </style>

    <style name="ShapeAppearance.PicCatcher.Rect" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">12dp</item>
    </style>

</resources>